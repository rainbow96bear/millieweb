const router = require("express").Router();

router.post("/getData", (req, res) => {
  //   const tempBooks = await .findAll({where:{category:req.body.category}})
  //   res.send({ booksArr: tempBooks });
});

module.exports = router;
